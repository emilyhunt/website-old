---
label: Custom Page
hide_body: false
fields:
- type: text
  name: title
  label: Title
  config:
    required: true
- name: url
  type: text
  config:
    required: false
  label: URL
  description: the page URL
- type: datetime
  name: date
  label: Date
- name: layout
  type: select
  default: custom
  config:
    required: false
    options:
    - custom
    - page
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Layout
- type: text
  name: description
  label: Description
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: thumbnail
  type: file
  config:
    maxSize: 64
  label: Thumbnail
- name: sections
  type: blocks
  label: Sections
  template_types:
  - hero
  - info
  - grid
  - cta
  - content
  config:
    min: 
    max: 
  description: Add different sections to build a custom page.

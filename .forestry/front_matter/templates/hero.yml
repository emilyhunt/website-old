---
label: hero
hide_body: true
display_field: heroHeading
fields:
- name: options
  type: field_group
  config: {}
  fields:
  - name: options
    type: include
    config: {}
    template: section
    label: Options
  label: Section Options
  description: Configure the section options like padding, width etc.
- name: height
  type: text
  config:
    required: false
  label: Height
  description: Set a height of the hero section in pixels. Example 800px
  default: auto
- name: alignHorizontal
  type: select
  default: left
  config:
    required: false
    options:
    - left
    - right
    - center
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Align Horizontal
- name: alignVertical
  type: select
  default: top
  config:
    required: false
    options:
    - top
    - middle
    - bottom
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Align Vertical
- name: headings
  type: field_group
  config: {}
  fields:
  - name: headings
    type: include
    config: {}
    template: headings
    label: Headings
  label: Headings
- name: background
  type: field_group
  config: {}
  fields:
  - name: backgroundImage
    type: file
    config:
      maxSize: 64
    label: Background Image
  - name: opacity
    type: number
    label: Background Image Opacity
    description: ''
    default: 1
    required: true
    config:
      min: 0
      max: 1
      step: 0.1
  - name: monotone
    type: boolean
    label: Monotone
    description: Blend the background image into the background using a monotone effect.
  label: Background
- name: image
  type: field_group
  config: {}
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: overlap
    type: boolean
    label: Overlap
  - name: border
    type: boolean
    label: Border
  - name: borderRadius
    type: boolean
    label: Border Radius
  - name: shadow
    type: boolean
    label: Shadow
  label: Image
- name: buttons
  type: field_group_list
  fields:
  - name: button
    type: include
    config: {}
    template: button
    label: Button
  config:
    min: 
    max: 
    labelField: text
  label: Buttons
